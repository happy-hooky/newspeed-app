import { TextStyle } from 'react-native';
import styled from 'styled-components/native';

const TextInput = styled.TextInput<TextStyle>`
  padding: 10px;
  height: 40,
  borderColor: 'gray',
  borderWidth: 1,
  padding: 10,
`;

export default TextInput;
